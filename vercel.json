{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs"
}

# 初始化git仓库（如果还没做）
git init
git add .
git commit -m "Initial commit for tarot app"

# 推送到GitHub
git remote add origin https://github.com/Ibook000/Tarot-Insight
git push -u origin main